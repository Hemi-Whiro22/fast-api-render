# Build tools
pip>=25.3
setuptools>=75.0.0
wheel

# FastAPI and web framework - Compatible versions
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
starlette>=0.27.0
python-multipart>=0.0.6

# Pydantic v2 (CRITICAL: Must be v2 for pydantic-settings compatibility)
pydantic>=2.5.0
pydantic-settings>=2.1.0

# HTTP clients and utilities  
httpx>=0.25.0
python-dotenv>=1.0.0

# AI/ML dependencies
openai>=1.3.0

# Database dependencies (CRITICAL MISSING DEPENDENCIES ADDED)
sqlalchemy>=2.0.0
psycopg[binary]>=3.1.0
pgvector>=0.2.3
supabase>=2.3.0

# For scripts (optional but used in workspace)
psycopg2-binary>=2.9.0
rich>=13.0.0

# Image processing
pillow>=10.0.0
pytesseract>=0.3.10